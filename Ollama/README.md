# Ollama and WebUI

## Start

Just run a simple command inside this directory.
`docker compose up`

## Prerequisities

* Docker `paru -Suy docker`
* Nvidia container toolkit `paru -Suy nvidia-container-toolkit` and `sudo nvidia-ctk runtime configure --runtime=docker`


